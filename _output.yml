bookdown::pdf_book:
  includes:
    header-includes:
    - \hypersetup{draft} # disable automatic hyperlink generation in pdf
bookdown::gitbook:
  split_by: chapter
  config:
    toc:
      collapse: section
